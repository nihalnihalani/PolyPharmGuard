# Project rules — see CLAUDE.md

This repo's project rules, architecture, conventions, build commands, and
agent-team strategy live in [`CLAUDE.md`](./CLAUDE.md).

`AGENTS.md` exists so non-Claude coding agents (Codex, Cursor, etc.) that
look for `AGENTS.md` find a pointer to the same source of truth instead of a
parallel doc that drifts. There is one canonical project rules file:
`CLAUDE.md`. Read it first.
